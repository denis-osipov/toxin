extends ../ui-kit/ui-kit.pug

block title
  include dependencies.pug

  ='Cards'

block content
  .ui-kit__grid-item
    +card('search')
    +card('registration')
  .ui-kit__grid-item
    +card('reservation', 888)
    +card('login')
  .ui-kit__grid-item
    +calendar
    +room-plate(888)
    +room-plate(840)