mixin radio(label)
  -
    let classes;
    if (attributes && attributes.class) {
      classes = attributes.class;
      delete attributes.class;
    }
    else {
      classes = [];
    }

  label.radio(class=classes)
    +field()(type='radio')&attributes(attributes)
    =label