//- Automatically generated imports.
//- Any changes in this block will be discarded during next compilation.
include ../button/button.pug
//- End of block with automatically generated imports.
mixin control(buttons, target)

  //- Create buttons using info in array of objects with properties:
  //-   text: string with button text
  //-   color: string with button text color
  //-   action: string with action after clicking (should correspond to some name in controlled block)
  //- and target - object with target info:
  //-   selector - string with jQuery selector for target element in DOM
  //-   name - data name for target object

  .control(data-selector=target.selector, data-name=target.name)&attributes(attributes)
    each button in buttons
      +button(
        {text: button.text, textColor: button.color},
        'text',
        'min')(
          class=`control__button control__button_type_${button.action}`,
          data-action=button.action)