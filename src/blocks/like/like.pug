//- Automatically generated imports.
//- Any changes in this block will be discarded during next compilation.
include ../input/input.pug
include ../icon/icon.pug
//- End of block with automatically generated imports.
mixin like(liked, number)
  -
    const likeClass = liked ? 'like like_liked_true' : 'like like_liked_false';
    const inputAttrs = {
      type:'checkbox',
      class: 'like__input',
      name: 'like'
    }
    if (liked) {
      inputAttrs.checked = '';
    }
  label(class=likeClass)&attributes(attributes)
    +input(false, 'invisible')&attributes(inputAttrs)
    +icon()(class='like__icon')
    span.like__count= number