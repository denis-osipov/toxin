//- Automatically generated imports.
//- Any changes in this block will be discarded during next compilation.
include ../button/button.pug
include ../icon/icon.pug
//- End of block with automatically generated imports.
mixin rating(stars)
  +button({}, 'text', 'min')(class='rating')&attributes(attributes)
    - let filled = 0;
    while filled < stars
      +icon('star', 'primary-gradient')(class='rating__icon')
      - filled++;
    - let empty = 5 - stars;
    while empty > 0
      +icon('star_border', 'primary-gradient')(class='rating__icon')
      - empty--;