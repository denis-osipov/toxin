mixin rating(stars)
  +button()(class='rating')
    - let filled = 0;
    while filled < stars
      +icon('star')
      - filled++;
    - let empty = 5 - stars;
    while empty > 0
      +icon('star_border')
      - empty--;