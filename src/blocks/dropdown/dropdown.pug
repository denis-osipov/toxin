//- Automatically generated imports.
//- Any changes in this block will be discarded during next compilation.
include __button/dropdown__button.pug
include __item/dropdown__item.pug
include __item-value/dropdown__item-value.pug
include __list/dropdown__list.pug
include __total/dropdown__total.pug
include ../control/control.pug
//- End of block with automatically generated imports.
mixin dropdown(name, type='', items, control=true)
  //- type should be one of:
  //-   guests
  //-   conveniences
  -
    const typeItems = {
      'guests': [
        'Взрослые',
        'Дети',
        'Младенцы'
      ],
      'conveniences': [
        'Спальни',
        'Кровати',
        'Ванные комнаты'
      ]
    };
    // Use given array of items or defaults
    const listItems = (items && items.length) ? items :
                      typeItems[type] ? typeItems[type] : [];
    const typeClass = type ? `dropdown_type_${type}` : '';

  .dropdown(class=typeClass)&attributes(attributes)
    if name
      h3.dropdown__header.text_level_h3= name
    +dropdown__total
    .dropdown__container
      +dropdown__list(listItems)
      if control
        +control(
          [
            { text: 'Очистить', color: 'dark-50', action: 'clear' },
            { text: 'Применить', color: 'primary-color', action: 'apply' }
          ],
          {
            selector: '.dropdown',
            name: 'dropdown'
          }
        )(class='dropdown__control')