mixin checkbox(name, description)
  -
    let classes;
    if (attributes && attributes.class) {
      classes = attributes.class;
      delete attributes.class;
    }
    else {
      classes = [];
    }

  label.checkbox(class=classes)
    +input()(type='checkbox', class='checkbox__input')&attributes(attributes)
    +checkbox__box
    span.checkbox__name= name
    if description
      span.checkbox__description= description