mixin list__item_type_icon(item)
  - const [iconName, itemName, description] = item
  +icon(iconName, 'primary-gradient')(class='list__item-icon')
  .list__item-text
    .list__item-label= itemName
    if description
      .list__item-description= description