//- Automatically generated imports.
//- Any changes in this block will be discarded during next compilation.
include ../avatar/avatar.pug
include ../like/like.pug
//- End of block with automatically generated imports.
mixin comment(options)
  //- options should contain:
  //-   userName - displayed user name,
  //-   date - how old message is,
  //-   message - comment text,
  //-   like - array [is liked (bool), number of likes (int)]
  article.comment&attributes(attributes)
    +avatar(options.userName)
    .comment__header
      .comment__user= options.userName
      .comment__date= options.date
    - const [liked, number] = options.like;
    +like(liked, number)(class='comment__like')
    p.comment__text= options.message